"""Tests for kdp-book.

Layout:
    unit/      — fast, deterministic tests (no network, no LLM calls)
    smoke/     — end-to-end pipeline runs against fixture/recorded responses
    fixtures/  — sample concepts, outlines, manuscripts, manifests
"""
